{% extends 'VoitureBackOfficeBundle::layout.html.twig' %}

	{% block content %}

		<div class="col-md-9">

			<h3>Liste des régions :</h3>
				{% for region in regions %}
					<a href="{{ path('voiture_back_office_editionRegions',{id:region.id}) }}">{{ region.name }}</a><br><br>

					<a href="{{ path('voiture_back_office_suppRegion',{id:region.id})}}" class="btn btn-primary">Supprimer cette région</a><br><br>
				{% endfor %}

		</div>

	{% endblock %}